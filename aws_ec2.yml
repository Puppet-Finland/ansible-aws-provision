---
plugin: amazon.aws.aws_ec2
region: eu-central-1
regions:
  - eu-central-1
keyed_groups:
  - key: placement.region
    prefix: aws_region
  - key: tags['role']
    prefix: "role"
  - key: tags['distro']
    prefix: "distro"
